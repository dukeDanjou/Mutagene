<section id="{{ content.id }}" class="sortable panel">

	<div class="panel-body">

		<form id="formContents_{{ content.id }}" action="{{ path('mgn_content', {'container': 'article', 'id': article.id, 'content': content.id, 'type': 'quote', 'action': 'save'})}}" method="post" {{ form_enctype(form_content) }} class="form-horizontal" role="form">

		    <div class="form-group">

			    <div class="col-lg-12">
			        
			        <label for="mgn_articlebundle_articletype_contenu">Citation <img class="loader" src="{{ asset('admin/img/ajax-loader.gif') }}" title="" /></label>

			        {{ form_widget(form_content.quote, { 'attr' : { 'class' : 'form-control textarea_elastic formcontent', 'rows' : '2' } }) }}

			    </div>

		    </div>

		    <div class="form-group">

			    <div class="col-lg-6">
			        
			        <label for="mgn_articlebundle_articletype_contenu">Nom</label>

			        {{ form_widget(form_content.quoteName, { 'attr' : { 'class' : 'form-control' } }) }}

			    </div>

			    <div class="col-lg-6">
			        
			        <label for="mgn_articlebundle_articletype_contenu">Lien</label>

			        {{ form_widget(form_content.quoteLink, { 'attr' : { 'class' : 'form-control' } }) }}

			    </div>

		    </div>
		        
		    {{ form_rest(form_content) }}

			<div class="pull-right">

				<button type="button" class="btn btn-primary btn-xs content-save">Enregistrer</button>

				<button type="button" class="btn btn-danger btn-xs content-delete" href="{{ path('mgn_content_delete', {'content': content.id})}}">Supprimer</button>

			</div>

		</form>

	</div>

</section>