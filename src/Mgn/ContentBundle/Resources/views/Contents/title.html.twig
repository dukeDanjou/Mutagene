<section id="{{ content.id }}" class="sortable panel">

	<div class="panel-body">

		<h2 style="color: #ff6c5f;">{{ content.title }}</h2>

		<p><a href="{{ path('mgn_content', {'container': 'article', 'id': article.id, 'content': content.id, 'type': 'title', 'action': 'edit'})}}" class="pull-right text-info content-edit"><small>Editer</small></a></p>

	</div>

</section>